Sidebar navigation row. Inactive rows are transparent with `--ink2` text; the active row gets a solid `--navactive` fill.

```jsx
<NavItem icon={<i data-lucide="layout-dashboard" />} label="Dashboard" active />
<NavItem icon={<i data-lucide="receipt-text" />} label="Transactions" />
```
