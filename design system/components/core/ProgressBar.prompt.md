Thin (5–6px) rounded track used under every envelope/category row to show spent-vs-budgeted. Automatically switches to the negative color and 100% width when over budget.

```jsx
<ProgressBar value={420} max={600} color="var(--cat-groc)" />
<ProgressBar value={310} max={300} color="var(--cat-shop)" />
```
